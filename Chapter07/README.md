# Chapter 7: MCP Clients without LLMs: Streams, State, and Resilience

## Overview

Code samples for Chapter 7. See the chapter text in `d:/gitbook/ModelContextProtocol/chapters/Ch07.md` for full explanations.

## Running the samples

```bash
cd code
dotnet run
```

## Code samples

See the `code/` directory. Files are named `ch07_N_description.cs`.

## Solutions

Quiz answers are in `solutions/solution-quiz.md`.
