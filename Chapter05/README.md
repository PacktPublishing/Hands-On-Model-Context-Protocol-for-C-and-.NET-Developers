# Chapter 5: Building the Travel Booking Server in ASP.NET Core

## Overview

Code samples for Chapter 5. See the chapter text in `d:/gitbook/ModelContextProtocol/chapters/Ch05.md` for full explanations.

## Running the samples

```bash
cd code
dotnet run
```

## Code samples

See the `code/` directory. Files are named `ch05_N_description.cs`.

## Solutions

Quiz answers are in `solutions/solution-quiz.md`.
